model Entreprise{
    id Int @id @default(autoincrement())
    raisonSociale String
    siret String @unique
    password String @db.VarChar(255)
    headName String?

    employees Employee[]
    cars Car[]
}